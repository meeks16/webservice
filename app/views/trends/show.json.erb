<% @trends.each do |user| %>{
   <%= @trend.term %>,
   <%= @trend.socialnetwork %>,
   <%= @trend.url %>,
<!--   data = { :person => { :term => "john", :last => "doe", :age => 30 }, :person => .. -->
}

    <% end %>